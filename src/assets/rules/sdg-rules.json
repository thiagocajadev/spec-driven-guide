{
  "version": 1,
  "exclude": [
    "**/*.generated.*",
    "**/migrations/**",
    "**/*.g.*",
    "**/node_modules/**",
    "**/*.min.*"
  ],
  "rules": [
    {
      "id": "explaining-returns",
      "tier": "BLOCK",
      "description": "Named const before every meaningful return. No call expression, ternary, or anonymous object on the return line itself. Exemption: void-terminator form, a function whose last statement is a side-effect call returning void/undefined (console.log, process.stdout.write, etc.) MAY end with that bare statement and an implicit return. Wrapping void calls into `const X = sideEffect(); return X;` is a ceremonial violation (see ceremonial-void-return).",
      "exampleViolation": "return Result.Ok(order);",
      "exampleFix": "const createdOrder = Result.Ok(order);\nreturn createdOrder;"
    },
    {
      "id": "taboo-verbs",
      "tier": "BLOCK",
      "description": "Function and method names must not use banned verbs: handle, do, run, execute, perform. Exception: 'handle' is allowed only for DOM event handlers (e.g., handleClick, handleSubmit).",
      "banned": ["handle", "do", "run", "execute", "perform"],
      "exampleViolation": "function handleOrderCreation() {}",
      "exampleFix": "function processOrderCreation() {}"
    },
    {
      "id": "boolean-prefix",
      "tier": "BLOCK",
      "description": "All boolean variables must carry a semantic prefix: is, has, can, should, did, needs, supports, allows.",
      "requiredPrefixes": [
        "is",
        "has",
        "can",
        "should",
        "did",
        "needs",
        "supports",
        "allows"
      ],
      "exampleViolation": "const active = user.status === 'active';",
      "exampleFix": "const isActive = user.status === 'active';"
    },
    {
      "id": "inline-assertion-literals",
      "tier": "BLOCK",
      "description": "No magic strings, template literals, or anonymous objects passed directly as assertion arguments. Extract into named consts first.",
      "exampleViolation": "assert.equal(actual, 0, `Orphans: ${list.join(', ')}`);",
      "exampleFix": "const failureMessage = `Orphans: ${list.join(', ')}`;\nassert.equal(actual, 0, failureMessage);"
    },
    {
      "id": "named-const-before-call",
      "tier": "BLOCK",
      "description": "No computed expression passed directly as an argument in any function call. Covered forms: (a) function/method calls, which include the high-frequency test idiom `assert.ok(actual.includes(expected))` / `assert.ok(actual.startsWith(expected))` / `assert.ok(actual.match(pattern))` / `assert.ok(obj.error.message.includes(text))`. The method call is the boolean subject and MUST be extracted to `const hasExpected = actual.includes(expected);` (or `hasPrefix`, `hasMatch`, etc.) first; zero tolerance even when the method argument is a plain literal, (b) ternaries, (c) template-literals with interpolation `${...}`, (d) concatenations with `+`, (e) anonymous object literals used as configuration, (f) binary-comparison expressions (`<`, `<=`, `>`, `>=`, `===`, `!==`, `==`, `!=`) passed as boolean subject, e.g. `assert.ok(actualBytes < 2700, ...)` must extract `const isUnderLimit = actualBytes < 2700;` first, (g) unary-negation of a call or property access (`!fn(x)`, `!arr.includes(x)`, `!obj.prop`) passed as boolean subject, e.g. `assert.ok(!actual.includes(pattern), ...)` must extract `const isPatternAbsent = !actual.includes(pattern);` first. Extract into a named const first so the callsite reads as a line of the process. Exempt: plain string literals ('utf8'), plain number literals (2), plain boolean literals, and direct function references (fs.existsSync), since these carry no composition and adding a named const for them is ceremony. Printer-function exemption: when the enclosing function body is exactly one call of `console.*` / `process.stdout.write` / `process.stderr.write` and the function name already carries the intent (`printX`, `logX`, `warnX`), the function name serves as the semantic const, so do not extract the template into an extra variable, it would duplicate the function's own naming. Test-framework title exemption: positional template-literal titles in consagrated APIs (`it(\\`should X ${const}\\`)`, `describe(\\`...\\`)`) are test-name composition, not process-narrative, so do not extract. Scope: all callsites (assert, throw, console.log, fs.*, execSync, and any user call). Violation is at the argument position, not in variable assignment RHS, object property values, or array elements. Extracted boolean consts must honor `boolean-prefix` (is/has/can/should/did/needs/supports/allows).",
      "exampleViolation": "console.log(`Update failed: ${error.message}`);\nfs.writeFileSync(path, JSON.stringify(data, null, 2) + '\\n');\nassert.ok(actualBytes < 2700, `Output is ${actualBytes} bytes`);\nassert.ok(!actual.includes(pattern), `Pattern leaked: ${pattern}`);\nassert.ok(actual.includes(expectedSubstring));\nassert.ok(actual.error.message.includes(expectedInMessage));",
      "exampleFix": "const failureOutput = `Update failed: ${error.message}`;\nconsole.log(failureOutput);\n\nconst serialized = JSON.stringify(data, null, 2) + '\\n';\nfs.writeFileSync(path, serialized);\n\nconst isUnderLimit = actualBytes < 2700;\nconst sizeMessage = `Output is ${actualBytes} bytes`;\nassert.ok(isUnderLimit, sizeMessage);\n\nconst isPatternAbsent = !actual.includes(pattern);\nconst leakMessage = `Pattern leaked: ${pattern}`;\nassert.ok(isPatternAbsent, leakMessage);\n\nconst hasExpectedSubstring = actual.includes(expectedSubstring);\nassert.ok(hasExpectedSubstring);\n\nconst hasExpectedInMessage = actual.error.message.includes(expectedInMessage);\nassert.ok(hasExpectedInMessage);"
    },
    {
      "id": "taboo-nouns",
      "tier": "BLOCK",
      "description": "Variable and parameter names must not be exactly a banned noun (standalone, not as part of a compound name). The full identifier must equal the banned word, so compound names like `packageData`, `ruleObj`, `existingItems` are not violations. Banned nouns: data, info, obj, item, thing, res, req, ctx, cb, fn, val, tmp, arr.",
      "banned": [
        "data",
        "info",
        "obj",
        "item",
        "thing",
        "res",
        "req",
        "ctx",
        "cb",
        "fn",
        "val",
        "tmp",
        "arr"
      ],
      "exampleViolation": "function saveData(obj) {}",
      "exampleFix": "function saveOrder(order) {}"
    },
    {
      "id": "sla-violation",
      "tier": "WARN",
      "description": "A function should either orchestrate (call named functions) or implement (compute/transform), not both. Functions that mix levels of abstraction violate the Single Level of Abstraction rule.",
      "exampleViolation": "function processOrder(order) { validateOrder(order); const total = order.items.reduce((sum, i) => sum + i.price, 0); }",
      "exampleFix": "function processOrder(order) { validateOrder(order); const total = computeOrderTotal(order); }"
    },
    {
      "id": "arrow-antipattern",
      "tier": "WARN",
      "description": "Deeply nested conditionals (3+ levels) instead of guard clauses. Prefer early returns to reduce nesting.",
      "exampleViolation": "if (a) { if (b) { if (c) { doWork(); } } }",
      "exampleFix": "if (!a) return;\nif (!b) return;\nif (!c) return;\ndoWork();"
    },
    {
      "id": "visual-density",
      "tier": "WARN",
      "description": "Logically distinct groups of statements must be separated by exactly one blank line. Zero blank lines within a group. Never two or more consecutive blank lines. Groups are defined by semantic intent, not by statement type.",
      "exampleViolation": "assert.ok(isArray(actual.block));\nassert.ok(isArray(actual.warn));\nassert.ok(actual.block.every(r => r.tier === 'BLOCK'));",
      "exampleFix": "assert.ok(isArray(actual.block));\nassert.ok(isArray(actual.warn));\n\nassert.ok(actual.block.every(r => r.tier === 'BLOCK'));"
    },
    {
      "id": "ceremonial-void-return",
      "tier": "WARN",
      "description": "Do not wrap lexically-void side-effect primitives into `const X = call(...); return X;`. Flagged call targets: console.log, console.error, console.warn, console.info, process.stdout.write, process.stderr.write, and any method whose name starts with `print` (print*, e.g. printUsage, BundleUI.printHelp). These are known statement-style terminators; the named const holds undefined forever and satisfies explaining-returns mechanically while destroying its intent. Fix: emit the bare side-effect statement and let the function return implicitly (use `return;` only when serving as early-return guard before further branches). Does NOT apply to orchestrators calling named subfunctions via the canonical 2-statement `Pure Entry Point` form. That form is project convention (see code-style.md PreStartGate) and stands even when the subfunction happens to return undefined. Scope: leaf sites where the RHS itself is one of the flagged primitives.",
      "exampleViolation": "function printUsage() {\n  const usageResult = console.log(message);\n  return usageResult;\n}",
      "exampleFix": "function printUsage() {\n  console.log(message);\n}"
    }
  ]
}
